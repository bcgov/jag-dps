server.servlet.context-path=/vipsnotificationworker
spring.rabbitmq.host: ${RABBITMQ_HOST:localhost}
spring.rabbitmq.port: ${RABBITMQ_PORT:5672}
spring.rabbitmq.username: ${RABBITMQ_USERNAME:guest}
spring.rabbitmq.password: ${RABBITMQ_PASSWORD:guest}
ords.figcr.client.base-path=${FIGCR_BASE_PATH}
ords.figcr.client.username=${FIGCR_USERNAME}
ords.figcr.client.password=${FIGCR_PASSWORD}

vips.sftp.host=${DPS_SFTP_HOST:localhost}
vips.sftp.port=${DPS_SFTP_PORT:22}
vips.sftp.username=${DPS_SFTP_USERNAME:admin}
vips.sftp.password=${DPS_SFTP_PASSWORD:admin}
vips.sftp.known-hosts=${DPS_SFTP_KNOWNHOSTS}